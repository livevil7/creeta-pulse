{
  "name": "pulse-agent-dashboard",
  "displayName": "Pulse — Real-time Agent Dashboard",
  "description": "Real-time multi-agent dashboard for Claude Code. Visualize parallel agent execution, live thinking process, and tool calls from the lens plugin.",
  "icon": "media/icon.png",
  "version": "0.5.0",
  "publisher": "CreetaCorp",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/CreetaCorp/pulse"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Visualization",
    "Other"
  ],
  "keywords": [
    "claude-code",
    "lens",
    "CreetaCorp",
    "multi-agent",
    "dashboard",
    "ai-agents"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "pulse.openDashboard",
        "title": "Open Agent Dashboard",
        "category": "Pulse",
        "icon": "$(radio-tower)"
      },
      {
        "command": "pulse.refreshDashboard",
        "title": "Refresh Dashboard",
        "category": "Pulse",
        "icon": "$(refresh)"
      },
      {
        "command": "pulse.clearDashboard",
        "title": "Clear Dashboard History",
        "category": "Pulse",
        "icon": "$(trash)"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "pulse-agents",
          "title": "Pulse Agents",
          "icon": "$(radio-tower)"
        }
      ]
    },
    "views": {
      "pulse-agents": [
        {
          "id": "pulseAgentTree",
          "name": "Agent Status",
          "contextualTitle": "Pulse Agent Dashboard"
        }
      ]
    },
    "menus": {
      "view/title": [
        {
          "command": "pulse.openDashboard",
          "when": "view == pulseAgentTree",
          "group": "navigation"
        },
        {
          "command": "pulse.refreshDashboard",
          "when": "view == pulseAgentTree",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "title": "Pulse Agent Dashboard",
      "properties": {
        "pulse.autoOpen": {
          "type": "boolean",
          "default": true,
          "description": "Automatically open the dashboard when agents start running."
        },
        "pulse.pollInterval": {
          "type": "number",
          "default": 500,
          "minimum": 100,
          "maximum": 5000,
          "description": "Dashboard polling interval in milliseconds."
        },
        "pulse.theme": {
          "type": "string",
          "default": "cyberpunk",
          "enum": ["cyberpunk"],
          "description": "Dashboard visual theme."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src --ext ts",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.4.0",
    "@vscode/vsce": "^3.0.0"
  }
}
